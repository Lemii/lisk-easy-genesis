export const paths = {
  config: "../output/config.json",
  genesisBlock: "../output/genesisBlock.json",
  credentials: "../output/credentials.json",
};

export const balance = {
  genesisAccount: BigInt(1000000000000000),
  genesisDelegate: BigInt(0),
};

export const onion = {
  count: 10000,
  distance: 1000,
};

// default options that are not configurable in baseConfig.js file
// note: these will be overruled when using the wizard prompt process

export const defaultOptions = {
  prefix: "genesis",
  accounts: 3, // non-delegate genesis accounts
};
